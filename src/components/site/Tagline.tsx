/** The brand tagline under the name: a plain mono eyebrow. Nothing loops. Server component. */
export function Tagline({ text }: { text: string }) {
  return <span className="brand-tag">{text}</span>;
}
